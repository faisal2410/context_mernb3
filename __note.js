/**
 * Context Api :
 * ==============
 * Create context using createContext eg. authContext=createContext()
 * create Provider function eg. AuthProvider which return AuthContext.Provider
 * Set Provider with value 
 *useAuth=> useContext(AuthContext)
 * export
 * Use Context in the place where you want to get the context 
 * */ 


//  "server":"json-server --watch database/db.json --port=8000 --delay 500"